# squadrons-chain-search

First-party Cordis tool `publish_chain_search` — turns live The Graph Subgraph MCP results into a chat GenUI card (`kind: squadrons.chain-search`).

The host injects Subgraph MCP when `THE_GRAPH_GATEWAY_API_KEY` is set. End users do not paste a key.
